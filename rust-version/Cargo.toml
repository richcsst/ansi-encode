[package]
name = "ansi-encode"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
once_cell = "1"
regex = "1"
unicode_names2 = "1"
unicode-width = "0.2"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
