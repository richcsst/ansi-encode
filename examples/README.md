# Term::ANSIEncode Examples

These files can be decoded by the ```ansi-encode``` binary to seer their output.  Note:  Some require a wide terminal to see properly.

Example:

```
ansi-encode examples/use.ans
```
